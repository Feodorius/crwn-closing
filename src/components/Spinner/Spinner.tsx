import { SpinnerOverlay, SpinnerContainer, } from "./spinner.styled";

const Spinner = () => {

    return (
        <SpinnerOverlay>
            <SpinnerContainer />
        </SpinnerOverlay>
    );
};

export default Spinner;